<UserControl x:Class="Ame.Modules.Docks.DockManager"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             xmlns:xcad="http://schemas.xceed.com/wpf/xaml/avalondock"
             xmlns:local="clr-namespace:Ame.Modules.Docks"
             xmlns:serializer="clr-namespace:Ame.Modules.Docks.Serializer"
             xmlns:maineditor="clr-namespace:Ame.Modules.MapEditor.Editor;assembly=Ame.Modules.MapEditor"
             xmlns:itemeditor="clr-namespace:Ame.Modules.Docks.ItemEditorDock"
             xmlns:itemlist="clr-namespace:Ame.Modules.Docks.ItemListDock"
             xmlns:clipboard="clr-namespace:Ame.Modules.Docks.ClipboardDock"
             xmlns:layerlist="clr-namespace:Ame.Modules.Docks.LayerListDock"
             xmlns:minimap="clr-namespace:Ame.Modules.Docks.MinimapDock"
             xmlns:toolbox="clr-namespace:Ame.Modules.Docks.ToolboxDock"
             xmlns:selectedbrush="clr-namespace:Ame.Modules.Docks.SelectedBrushDock"
             xmlns:sessionview="clr-namespace:Ame.Modules.Docks.SessionViewerDock"
             xmlns:conv="clr-namespace:Ame.Infrastructure.Converters;assembly=Ame.Infrastructure"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d"
             d:DesignHeight="300"
             d:DesignWidth="300">

    <UserControl.Resources>
        <conv:ActiveDocumentConverter x:Key="ActiveDocumentConverter" />
        <conv:BooleanToVisiblityConverter x:Key="BoolToVisConverter"
                                          Collapse="True" />
    </UserControl.Resources>

    <xcad:DockingManager x:Name="dockManager"
                         DocumentsSource="{Binding Documents}"
                         AnchorablesSource="{Binding Anchorables}"
                         ActiveContent="{Binding ActiveDocument, Mode=TwoWay, Converter={StaticResource ActiveDocumentConverter}}"
                         serializer:DockLayoutSerializer.LoadLayoutCommand="{Binding DockLayout.LoadLayoutCommand}"
                         serializer:DockLayoutSerializer.SaveLayoutCommand="{Binding DockLayout.SaveLayoutCommand}"
                         Grid.Row="2">

        <xcad:DockingManager.LayoutItemTemplateSelector>
            <local:DockTemplateSelector>
                <local:DockTemplateSelector.ClipboardDataTemplate>
                    <DataTemplate>
                        <clipboard:Clipboard />
                    </DataTemplate>
                </local:DockTemplateSelector.ClipboardDataTemplate>
                <local:DockTemplateSelector.MainEditorTemplate>
                    <DataTemplate>
                        <maineditor:MainEditor />
                    </DataTemplate>
                </local:DockTemplateSelector.MainEditorTemplate>
                <local:DockTemplateSelector.ItemEditorDataTemplate>
                    <DataTemplate>
                        <itemeditor:ItemEditor />
                    </DataTemplate>
                </local:DockTemplateSelector.ItemEditorDataTemplate>
                <local:DockTemplateSelector.ItemListDataTemplate>
                    <DataTemplate>
                        <itemlist:ItemList />
                    </DataTemplate>
                </local:DockTemplateSelector.ItemListDataTemplate>
                <local:DockTemplateSelector.LayerListDataTemplate>
                    <DataTemplate>
                        <layerlist:LayerList />
                    </DataTemplate>
                </local:DockTemplateSelector.LayerListDataTemplate>
                <local:DockTemplateSelector.MinimapDataTemplate>
                    <DataTemplate>
                        <minimap:Minimap />
                    </DataTemplate>
                </local:DockTemplateSelector.MinimapDataTemplate>
                <local:DockTemplateSelector.SelectedBrushDataTemplate>
                    <DataTemplate>
                        <selectedbrush:SelectedBrush />
                    </DataTemplate>
                </local:DockTemplateSelector.SelectedBrushDataTemplate>
                <local:DockTemplateSelector.ToolboxDataTemplate>
                    <DataTemplate>
                        <toolbox:Toolbox />
                    </DataTemplate>
                </local:DockTemplateSelector.ToolboxDataTemplate>
                <local:DockTemplateSelector.SessionViewDataTemplate>
                    <DataTemplate>
                        <sessionview:SessionViewer />
                    </DataTemplate>
                </local:DockTemplateSelector.SessionViewDataTemplate>
            </local:DockTemplateSelector>
        </xcad:DockingManager.LayoutItemTemplateSelector>

        <xcad:DockingManager.LayoutItemContainerStyleSelector>
            <local:DockStyleSelector>
                <local:DockStyleSelector.DockWindowStyle>
                    <Style TargetType="{x:Type xcad:LayoutAnchorableItem}">
                        <Setter Property="Title"
                                Value="{Binding Model.Title}" />
                        <Setter Property="Visibility"
                                Value="{Binding Model.IsVisible, Mode=TwoWay, Converter={StaticResource BoolToVisConverter}, ConverterParameter={x:Static Visibility.Hidden}}" />
                        <Setter Property="IsSelected"
                                Value="{Binding Model.IsSelected, Mode=TwoWay}" />
                        <Setter Property="IsActive"
                                Value="{Binding Model.IsActive, Mode=TwoWay}" />
                        <Setter Property="ContentId"
                                Value="{Binding Model.ContentId}" />
                    </Style>
                </local:DockStyleSelector.DockWindowStyle>
                <local:DockStyleSelector.DocumentStyle>
                    <Style TargetType="{x:Type xcad:LayoutItem}">
                        <Setter Property="Title"
                                Value="{Binding Model.Title}" />
                        <Setter Property="Visibility"
                                Value="{Binding Model.IsVisible, Mode=TwoWay, Converter={StaticResource BoolToVisConverter}, ConverterParameter={x:Static Visibility.Hidden}}" />
                        <Setter Property="IsSelected"
                                Value="{Binding Model.IsSelected, Mode=TwoWay}" />
                        <Setter Property="IsActive"
                                Value="{Binding Model.IsActive, Mode=TwoWay}" />
                        <Setter Property="ContentId"
                                Value="{Binding Model.ContentId}" />
                    </Style>
                </local:DockStyleSelector.DocumentStyle>
            </local:DockStyleSelector>
        </xcad:DockingManager.LayoutItemContainerStyleSelector>
    </xcad:DockingManager>
</UserControl>
